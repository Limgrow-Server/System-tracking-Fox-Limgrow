enum MappingStatus {
  ACTIVE   @map("active")
  INACTIVE @map("inactive")
  ARCHIVED @map("archived")

  @@map("mapping_status")
}

enum CredentialStatus {
  ACTIVE   @map("active")
  DISABLED @map("disabled")

  @@map("credential_status")
}

enum SecretFormat {
  JSON @map("json")
  P8   @map("p8")

  @@map("secret_format")
}

enum CredentialPurpose {
  FIREBASE_ADMIN @map("firebase_admin")
  REVIEW         @map("review")
  IAP            @map("iap")

  @@map("credential_purpose")
}

enum ReviewSyncStatus {
  IDLE      @map("idle")
  RUNNING   @map("running")
  SUCCEEDED @map("succeeded")
  FAILED    @map("failed")

  @@map("review_sync_status")
}

enum ReviewFetchRunStatus {
  RUNNING   @map("running")
  SUCCEEDED @map("succeeded")
  FAILED    @map("failed")
  PARTIAL   @map("partial")

  @@map("review_fetch_run_status")
}

enum ReviewFetchTrigger {
  SCHEDULED @map("scheduled")
  MANUAL    @map("manual")
  RETRY     @map("retry")

  @@map("review_fetch_trigger")
}

enum ReviewFetchScheduleStatus {
  ACTIVE @map("active")
  PAUSED @map("paused")

  @@map("review_fetch_schedule_status")
}
